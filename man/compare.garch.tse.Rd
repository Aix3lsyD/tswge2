% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.garch.tse.R
\name{compare.garch.tse}
\alias{compare.garch.tse}
\title{Compare Multiple GARCH Model Specifications}
\usage{
compare.garch.tse(
  data,
  arch.range = 0:2,
  garch.range = 0:2,
  distribution = "norm",
  include.mean = FALSE,
  solver = "hybrid"
)
}
\arguments{
\item{data}{Numeric vector of returns or residuals to model.}

\item{arch.range}{Integer vector of ARCH orders to try (default 0:2).}

\item{garch.range}{Integer vector of GARCH orders to try (default 0:2).}

\item{distribution}{Character string specifying the innovation distribution.
Default is "norm".}

\item{include.mean}{Logical. Include a mean term in the model?
Default is FALSE.}

\item{solver}{Character string for optimization solver. Default is
"hybrid" which tries multiple solvers.}
}
\value{
A list of class "garch.comparison.tse" containing fits (list of
uGARCHfit objects), comparison (tibble with model statistics),
distribution, and n.
}
\description{
Fits a grid of ARCH/GARCH models to a time series and computes information
criteria and diagnostic statistics for model comparison and selection.
}
\details{
The function fits all combinations of ARCH and GARCH orders (excluding
the trivial (0,0) case) and computes AIC, AICc, BIC, Weighted Ljung-Box
tests, Nyblom stability test, and Sign Bias test.
}
\examples{
\dontrun{
garch_gen <- make.gen.garch.tse(omega = 0.1, alpha = 0.15, beta = 0.8)
y <- garch_gen(1000)
results <- compare.garch.tse(y)
table.garch.gt.tse(results)
}
}
